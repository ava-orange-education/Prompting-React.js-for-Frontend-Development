# Chapter 01 — Code Snippets

Extracted from `512_01_Final.docx`. Code blocks are reproduced in document order.


## Code files

- `App.tsx` — AI will typically respond that the file belongs at src/App.tsx and give you the following code:
- `App-2.tsx` — AI will likely give you the following:
- `App-3.tsx` — The expected upgrade is:

## Terminal commands


AI will typically reply with the following commands:

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app
npm install
npm run dev
```

AI would suggest checking your versions with the following commands and would recommend Node 18 or later for compatibility:

```bash
node -v
npm -v
```
